"use strict";
const str1 = prompt("Enter the first line:");
const str2 = prompt("Enter the second line:");
const str3 = prompt("Enter the third line:");

alert("Result: " + str3 + ", " + str1 + ", " + str2);
